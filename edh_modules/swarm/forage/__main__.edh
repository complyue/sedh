
import * 'swarm/forager'


case forageOn(
  10, # headcount - number of worker processes to vend
  '', # targetPrefix - prefix to filter out uninteresting work
  # local addr to sniff for call-for-workers announced to the swarm
  '0.0.0.0', 3722,
) of { sniffer } -> { pass }


# keep the forager process running in case this file is redirected
# as stdin
sniffer.join()
